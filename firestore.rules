rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if request.auth == null && request.headers.referer.matches('https://botti-23428.web.app/.*');
      allow write: if false;
    }
  }
}